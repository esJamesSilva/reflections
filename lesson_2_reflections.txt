What happens when you initialize a repository? Why do you need to do it?

It create the directory .git which have files used by git to register the commits nexts
becouse git create  the folder .git

How is the staging area different from the working directory and the repository? what value do you think it offers?
the staging area is a area that let indicate that files going being checkout together.
The value that offers is that let ckeckout join.


How can you use the staging area to make sure you have one commit per logical change?
Add the file to stage area and commit later.

What are some situations when branches would be helpful in keeping you history organized? How would branches help?
When I want to separte projects or when I want try news features

How do the diagrams help you visualize the branch structure?
Me ayudan a entender el flujo de cambios realizados y como funciona el sistema de commits en git

What is the result of merging two branches together? why do we represent it in the diagram the way do?
la union de los cambios en una de las branch por que se base en dos branches para hacerlo.

What are the pros and cons fo Git's automatic merging vs. always doing merges manually?
pros - rapidez cons- > no se controla que se guarda
